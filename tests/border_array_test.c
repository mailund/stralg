#include <stdlib.h>
#include <string.h>
#include <stralg.h>

int main(int argc, char * argv[])
{
    const char * test_str = "abababa";
    unsigned int n = strlen(test_str);
    unsigned int b[n];
    
    return EXIT_SUCCESS;
}
